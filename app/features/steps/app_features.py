from behave import given, when, then

@given("we have multiple CSV found in a directory")
def step_impl(context):
    pass

@when("the user provide input and select a CSV to work with")
def step_impl(context):
    pass

@then("the CSV file is parsed and the application ask for the column to order in string descending order")
def step_impl(context):
    pass
